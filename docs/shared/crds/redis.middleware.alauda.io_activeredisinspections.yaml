---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: activeredisinspections.redis.middleware.alauda.io
spec:
  group: redis.middleware.alauda.io
  names:
    kind: ActiveRedisInspection
    listKind: ActiveRedisInspectionList
    plural: activeredisinspections
    singular: activeredisinspection
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ActiveRedisInspection is the Schema for the ari API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ActiveRedisInspectionSpec defines the desired state of ActiveRedisInspection
            properties:
              addresses:
                description: |-
                  Addresses is a list of redis addresses for the active redis connection.
                  It can be a single address or multiple addresses.
                items:
                  type: string
                maxItems: 15
                minItems: 1
                type: array
              enableTLS:
                description: EnableTLS enable tls for redis connection.
                type: boolean
              instance:
                description: Instance is the instance name of local redis.
                minLength: 1
                type: string
              secretName:
                description: |-
                  SecretName the secret name for redis authentication. for default user,
                  the password field is required. Optional: only the 6.0 legacy module
                  authenticates the probe with this secret; the new (7.2) module uses
                  the instance-level peer-auth binding and ignores it.
                type: string
              serviceID:
                description: |-
                  ServiceID specifies the unique identifier for this Redis instance in an
                  active-active setup. Optional: the inspection prefers the instance's own
                  spec.activeRedis.serviceID; this field only fills in when the instance
                  declares none.
                  Must be unique across all Active-Active Redis instances.
                format: int32
                maximum: 15
                minimum: 0
                type: integer
            required:
            - addresses
            - instance
            type: object
          status:
            description: ActiveRedisInspectionStatus defines the observed state of
              ActiveRedisInspection
            properties:
              conditions:
                description: Conditions represents the current conditions of the ActiveRedisInspection.
                items:
                  description: Condition represents the state of a resource at a certain
                    point in time.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human-readable message indicating
                        details about the condition.
                      type: string
                    reason:
                      description: Reason is a brief reason for the condition's last
                        transition.
                      type: string
                    status:
                      description: Status is the status of the condition, one of "True",
                        "False", or "Unknown".
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              message:
                description: Message provides additional information about the current
                  state of the ActiveRedisInspection.
                type: string
              phase:
                description: Phase indicates the current phase of the ActiveRedisInspection.
                type: string
            type: object
        type: object
    served: true
    storage: true
